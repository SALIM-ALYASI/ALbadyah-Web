# ==============================================
# إعدادات البيئة للإنتاج (Production Environment)
# ==============================================

# معلومات التطبيق الأساسية
APP_NAME="البادية - Al-Badyah"
APP_ENV=production
APP_KEY=base64:yWka+8mJFbaYw0s6YJnSNOx792BabukkqE/8HI5JmIw=
APP_DEBUG=false
APP_URL=https://your-domain.com

# إعدادات اللغة
APP_LOCALE=ar
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=ar_SA

# إعدادات الصيانة
APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# إعدادات الخادم
PHP_CLI_SERVER_WORKERS=4

# إعدادات التشفير
BCRYPT_ROUNDS=12

# إعدادات السجلات
LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# إعدادات قاعدة البيانات
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=u145701829_albadya
DB_USERNAME=u145701829_albadya
DB_PASSWORD=Kjf7+W3zT3

# إعدادات الجلسات
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null
SESSION_SECURE_COOKIE=true

# إعدادات البث
BROADCAST_CONNECTION=log

# إعدادات نظام الملفات
FILESYSTEM_DISK=public

# إعدادات الطوابير
QUEUE_CONNECTION=database

# إعدادات التخزين المؤقت
CACHE_STORE=database
# CACHE_PREFIX=

# إعدادات Memcached
MEMCACHED_HOST=127.0.0.1

# إعدادات Redis
REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# إعدادات البريد الإلكتروني
MAIL_MAILER=smtp
MAIL_SCHEME=null
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS="noreply@your-domain.com"
MAIL_FROM_NAME="${APP_NAME}"

# إعدادات AWS (اختيارية)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

# إعدادات Vite
VITE_APP_NAME="${APP_NAME}"

# سر لوحة التحكم
ADMIN_SECRET=admin123

# إعدادات إضافية للإنتاج
SANCTUM_STATEFUL_DOMAINS=your-domain.com,www.your-domain.com

# ==============================================
# ملاحظات مهمة للإنتاج:
# ==============================================

# 1. تأكد من تغيير APP_URL إلى النطاق الفعلي
# 2. تأكد من تغيير MAIL_FROM_ADDRESS إلى بريدك الإلكتروني
# 3. تأكد من تحديث SANCTUM_STATEFUL_DOMAINS
# 4. تأكد من إعداد APP_DEBUG=false
# 5. تأكد من إعداد LOG_LEVEL=error أو warning
# 6. تأكد من إنشاء storage link: php artisan storage:link
# 7. تأكد من تشغيل: php artisan config:cache
# 8. تأكد من تشغيل: php artisan route:cache
# 9. تأكد من تشغيل: php artisan view:cache
